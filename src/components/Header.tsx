export function Header() {
  return (
    <header className="mast">
      <div className="mast-brand">
        <span className="mark" aria-hidden="true" />
        <div>
          <p className="eyebrow">SMF Works · Human-AI lab</p>
          <h1>Skill Card</h1>
        </div>
      </div>
      <p className="lede">
        Paste a SKILL.md. Print a one-pager. Share the playbook as a card — not
        the secrets, not the whole file.
      </p>
    </header>
  );
}
